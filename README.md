# Z_PUBLIC_ABAPGIT_TEST_REPOSITORY
A PUBLIC test repository to test the mcp-tool `abap_git_pull` (w/o PAT) (see [sapwebgui.mcp](https://github.com/Hochfrequenz/sapwebgui.mcp)). 
It doesn't contain meaningful data.
It just contains an empty package `Z_PUBLIC_ABAPGIT_TEST_PACKAGE` and a little report to call the `git pull` command from a single transaction without navigating through the entire abapGit UI.
It's just to test the `git pull` WITHOUT providing a github username or PAT.
For tests without a PAT use [Z_PRIVATE_ABAPGIT_TEST_REPOSITORY](https://github.com/Hochfrequenz/Z_PRIVATE_ABAPGIT_TEST_REPOSITORY).
